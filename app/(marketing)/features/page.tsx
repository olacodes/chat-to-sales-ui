import { FeaturesPageContent } from '@/components/marketing/FeaturesPage';

export const metadata = {
  title: 'Features — ChatToSales',
  description:
    'AI follow-ups in Nigerian English, Paystack-native payments, offline mode, voice note transcription, and 10 more features built for how Nigerian merchants actually sell.',
};

export default function FeaturesPage() {
  return <FeaturesPageContent />;
}
