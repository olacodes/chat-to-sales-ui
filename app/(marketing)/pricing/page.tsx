import { PricingPageContent } from '@/components/marketing/PricingPage';

export const metadata = {
  title: 'Pricing — ChatToSales',
  description:
    'Simple pricing starting from ₦5,000/month or pay per completed order. No hidden fees.',
};

export default function PricingPage() {
  return <PricingPageContent />;
}
