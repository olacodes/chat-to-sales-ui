import { ProductPageContent } from '@/components/marketing/ProductPage';

export const metadata = {
  title: 'Product — ChatToSales',
  description:
    'See everything ChatToSales can do: unified inbox, instant order capture, AI replies, in-chat payments, and more.',
};

export default function ProductPage() {
  return <ProductPageContent />;
}
